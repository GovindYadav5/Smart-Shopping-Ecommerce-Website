import { SmartShopValidators } from './smart-shop-validators';

describe('SmartShopValidators', () => {
  it('should create an instance', () => {
    expect(new SmartShopValidators()).toBeTruthy();
  });
});
